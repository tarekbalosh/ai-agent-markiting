# خطة بناء الوكيل التسويقي المستقل (Autonomous AI Agent) - CoreLogic Systems

هذا الملف يحتوي على الأوامر (Prompts) لبناء وكيل ذكي مستقل يعمل يومياً دون تدخل، من تطوير الواجهة إلى الخوادم والجدولة.

## المرحلة الأولى: هيكلة قاعدة البيانات للإدارة المستقلة (Prisma Schema)
**البرومبت الأول:**
> "أريد بناء وكيل ذكي تسويقي مستقل لشركة CoreLogic Systems باستخدام Node.js و PostgreSQL مع Prisma. قم بكتابة `schema.prisma` تتضمن الجداول التالية:
> 1. `CampaignConfig`: لتخزين الإعدادات العامة (الجمهور المستهدف، أهداف الشركة، نبرة الصوت).
> 2. `AgentTask`: لجدولة مهام الوكيل (توليد، نشر، تحليل) مع تحديد حالة المهمة (Pending, Completed, Failed).
> 3. `GeneratedContent`: لتخزين المحتوى المولد (نصوص، إيميلات).
> 4. `PlatformIntegration`: لتخزين توكنات (Tokens) الربط مع المنصات (LinkedIn, Twitter, Email SMTP).
> 5. `ActivityLog`: لتسجيل كل حركة يقوم بها الوكيل لغرض الإشراف.
> يرجى تزويدي بكود السكيما."

## المرحلة الثانية: بناء "عقل الوكيل" (Agentic Workflow & AI)
**البرومبت الثاني:**
> "في خادم Node.js، أريد بناء 'عقل' الوكيل المستقل باستخدام واجهة برمجة تطبيقات (Gemini/OpenAI). الوكيل يجب أن يعمل كمدير تسويق لشركة برمجيات.
> اكتب كود خدمة (Service) تقوم بالخطوات التالية يومياً:
> 1. قراءة `CampaignConfig` لفهم الهدف الحالي (مثلاً: بيع أنظمة أتمتة).
> 2. توليد خطة عمل يومية (مثال: كتابة بوست للينكدإن + كتابة رسالة بريد ترويجي).
> 3. توليد المحتوى الفعلي بناءً على الخطة.
> يرجى تزويدي بكود يوضح كيف يتخذ الوكيل القرار (Decision Making Logic) وكيف يولد المحتوى المتوافق مع سياق الشركة، وحفظ النتائج في قاعدة البيانات."

## المرحلة الثالثة: الجدولة الزمنية والأتمتة (Cron Jobs & Automation)
**البرومبت الثالث:**
> "النظام يجب أن يعمل دون تدخلي. قم بكتابة كود في Node.js باستخدام مكتبة مثل `node-cron` أو `BullMQ`. 
> أريد إعداد مهمة مجدولة (Cron Job) تعمل كل يوم الساعة 9:00 صباحاً. هذه المهمة يجب أن تستدعي 'عقل الوكيل' (الذي بنيناه في الخطوة السابقة) ليقوم بتوليد المحتوى اليومي. 
> بعد ذلك، أريد مهمة أخرى تعمل الساعة 12:00 ظهراً لأخذ المحتوى المولد (الذي حالته Approved أو جاهز للنشر) وتجهيزه للنشر التلقائي. يرجى كتابة كود الجدولة مع معالجة الأخطاء (Error Handling) وتسجيلها في جدول `ActivityLog`."

## المرحلة الرابعة: النشر التلقائي (Auto-Publishing & Integrations)
**البرومبت الرابع:**
> "الآن نريد أن يقوم الوكيل بالتنفيذ. قم بكتابة كود Node.js لربط الوكيل بالمنصات التالية:
> 1. إرسال بريد إلكتروني (Cold Email) باستخدام `Nodemailer` أو `Resend API` للعملاء المحتملين.
> 2. النشر التلقائي على صفحة LinkedIn باستخدام LinkedIn API.
> الوكيل يجب أن يقرأ المحتوى المجدول لليوم من قاعدة البيانات وينفذ عملية النشر أو الإرسال، ثم يحدّث حالة المحتوى إلى 'Published'. يرجى توفير كود دوال الربط (Integration Functions)."

## المرحلة الخامسة: لوحة تحكم الإشراف (Supervisor Dashboard in Next.js)
**البرومبت الخامس:**
> "أريد بناء لوحة تحكم (Dashboard) للإشراف فقط باستخدام Next.js (App Router) و Tailwind CSS. اللوحة يجب أن تحتوي على:
> 1. صفحة تعرض `ActivityLog` لمعرفة ماذا فعل الوكيل اليوم (متى استيقظ، ماذا ولّد، ماذا نشر).
> 2. صفحة لعرض المحتوى المولد (للقراءة فقط أو التعديل الطارئ).
> 3. صفحة لإعدادات الوكيل (تعديل الـ Prompts الأساسية للشركة، وتحديث الـ API Keys).
> يرجى كتابة مكونات React الأساسية لهذه اللوحة وكيفية جلب البيانات من الـ Backend."

## المرحلة السادسة: إعدادات الخادم للعمل المستمر (Deployment)
**البرومبت السادس:**
> "بما أن الوكيل يعمل في الخلفية (Background Tasks / Cron Jobs)، فإنه يحتاج لاستضافة خادم لا ينام. 
> يرجى إعطائي إعدادات الرفع (Deployment) على منصة Render لخادم Node.js، مع التركيز على كيفية إبقاء الخادم يعمل (Background Worker) لضمان تنفيذ الجدولة الزمنية. وكيفية رفع لوحة تحكم Next.js على Vercel لغرض الإشراف."
